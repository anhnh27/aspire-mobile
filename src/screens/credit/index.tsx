import * as React from 'react';
import {View} from 'react-native';

type CreditScreenProps = {};

const CreditScreen: React.FC = (props: CreditScreenProps) => {
  return <View />;
};

export default CreditScreen;
