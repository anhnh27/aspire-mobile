import * as React from 'react';
import {View} from 'react-native';

type PaymentScreenProps = {};

const PaymentScreen: React.FC = (props: PaymentScreenProps) => {
  return <View />;
};

export default PaymentScreen;
